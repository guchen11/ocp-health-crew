# ===========================================================================
# CNV HealthCrew AI - Configuration
# ===========================================================================
# Copy this file to ~/.config/cnv-healthcrew/config.env and edit it.
# All values below are examples -- update them for your environment.
# ===========================================================================

# ---------------------------------------------------------------------------
# Cluster Connection (Required)
# ---------------------------------------------------------------------------
# The remote host that has oc/kubectl access to your OpenShift cluster.
# This machine is accessed via SSH, and oc commands are run remotely.
RH_LAB_HOST=your-lab-host.example.com
RH_LAB_USER=root

# Path to your SSH private key (on THIS machine, not the remote host)
SSH_KEY_PATH=~/.ssh/id_ed25519

# KUBECONFIG path on the REMOTE host
KUBECONFIG_REMOTE=/home/kni/clusterconfigs/auth/kubeconfig

# ---------------------------------------------------------------------------
# Jumphost / Bastion (Optional)
# ---------------------------------------------------------------------------
# If the target host is behind a jumphost, configure it here.
# Connection path: You → Jumphost → Target Host
# Leave JUMPHOST_HOST empty for direct connections (no jumphost).
JUMPHOST_HOST=
JUMPHOST_USER=
JUMPHOST_KEY_PATH=~/.ssh/id_ed25519_jumphost

# ---------------------------------------------------------------------------
# Email Notifications (Optional)
# ---------------------------------------------------------------------------
# Leave EMAIL_TO empty to disable email notifications.
EMAIL_TO=
EMAIL_FROM=cnv-healthcrew@redhat.com
SMTP_SERVER=smtp.corp.redhat.com
SMTP_PORT=25

# ---------------------------------------------------------------------------
# AI-Powered Root Cause Analysis (Optional)
# ---------------------------------------------------------------------------
# Google Gemini API key for AI-powered analysis.
# Get one at: https://aistudio.google.com/apikey
# Leave empty to disable AI features.
GOOGLE_API_KEY=

# ---------------------------------------------------------------------------
# Web Dashboard (Optional)
# ---------------------------------------------------------------------------
# Host and port for the Flask web dashboard.
# 0.0.0.0 = listen on all interfaces (accessible from network)
# 127.0.0.1 = localhost only
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# Secret key for session cookies (change this in production!)
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-key-change-in-production

# ---------------------------------------------------------------------------
# Multi-User & Build Queue (Optional)
# ---------------------------------------------------------------------------
# Maximum number of health check builds that can run simultaneously.
MAX_CONCURRENT_BUILDS=3

# Database URL (default: SQLite in data directory)
# For PostgreSQL: postgresql://user:pass@host:5432/healthcrew
# DATABASE_URL=sqlite:///healthcrew.db
